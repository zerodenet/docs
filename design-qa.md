# ZeroDeNet 首页设计验收

## 验收范围

- 参考方向：`C:\Users\higanbana\.codex\generated_images\019f9318-df91-7f60-b720-0ec95659e9c3\call_nVmmPkr5O1usFWwewAWrGP46.png`
- 对照图：`C:\Users\higanbana\.codex\visualizations\2026\07\24\019f9318-df91-7f60-b720-0ec95659e9c3\zerodenet-design-comparison-v2.png`
- 桌面视口：1440 × 1024
- 移动视口：390 × 844
- 状态：浅色、深色、移动导航、站内主入口

## 迭代记录

### 第一轮

- P1 · 交互密度：首页“正在建设”每一行都有链接，项目名称与“进入文档”重复可点击，社区区块还有重复的项目入口。信息层级被过多链接打散。
- P1 · 按钮对齐：主题按钮继承默认行高，中文文字的光学位置偏上。
- 修正：建设方向改为纯信息列表；每个项目仅保留一个文档入口；社区只保留 GitHub；按钮统一使用 `inline-flex`、双轴居中和固定行高。

### 第二轮

- 桌面浅色：品牌渐变、标题层级、留白、分隔线和连续列表与参考方向一致，没有额外卡片堆叠。
- 桌面深色：品牌色、正文对比度、分隔线和按钮状态正常。
- 移动端：标题、说明和按钮自然换行，列表与项目区没有横向溢出；实测 `scrollWidth === clientWidth`。
- 内容：公开协议独立实现、易理解易使用、性能与可靠性三层定位表达完整；未加入内部讨论中的类比名称。
- 交互：首屏项目入口可导航至 `/projects/`；移动导航可操作；浏览器控制台无 error 或 warning。
- 可访问性：单一 H1、分区标题、导航标签、主题切换标签和可见焦点样式均保留；移动按钮高度为 48px。
- 资产：页面不需要新增位图或插画，不存在占位图、CSS 假图或自制 SVG 替代。
- 有意偏差：根据用户反馈，建设方向不再保留参考图中的逐行箭头和链接，这是信息降噪，不是遗漏。

## 自动检查

- `pnpm check`：通过，84 个 Markdown 文件及项目注册、链接、锚点、导航和页面可达性正常。
- `pnpm build`：通过，VitePress 客户端、服务端、静态页面和 sitemap 均成功生成。

## 最终结果

passed
